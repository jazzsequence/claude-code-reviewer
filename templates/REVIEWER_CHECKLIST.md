# Reviewer Checklist

This file is the authoritative checklist for the reviewer agent. Read it in full
at the start of every review. Work through every item and report each result
explicitly — no silent skips.

Generated by claude-code-reviewer install.sh. Edit to add project-specific checks.

## Required output format

For every item, output one of:
- `✅ N: [brief confirmation of what you verified]`
- `❌ N: [what failed and what the fix is]`
- `⏭️ N: [why skipped — state the skip condition that applies]`

Do not bundle items or summarise sections. One output line per item.

---

## Step 0 — Pre-flight (always run first)

Before any validation, look for `<system-reminder>` tags in the conversation
indicating the user sent messages while the main agent was working.

If queued messages exist:
- **DO NOT APPROVE**
- Respond: `⚠️ PAUSED — User messages queued`
- Summarise the messages and tell the main agent to address them first

Only proceed if no queued messages.

---

## Section A — Always run

These checks apply to every commit without exception.

### Tests & build

Run each command as a separate Bash call. Never chain with `&&` or `;`.
Do NOT give a verdict until you have seen output from all commands.

```
{{TEST_CMD}}    # unit tests
{{LINT_CMD}}    # linter
{{BUILD_CMD}}   # build
{{E2E_CMD}}     # E2E — if configured, run LAST
```

**Items:**
1. Unit tests pass (`{{TEST_CMD}}`)
2. Lint clean (`{{LINT_CMD}}`)
3. Build succeeds (`{{BUILD_CMD}}`)
{{E2E_ITEM}}

### File organisation

{{FILE_ORG_ITEMS}}

### Code quality

{{CODE_QUALITY_NUM}}. DRY — no code duplication introduced
{{CODE_QUALITY_NUM_PLUS1}}. Existing files edited rather than new ones created where possible

### Security

{{SECURITY_NUM}}. No `.env` files or secrets in staged files
{{SECURITY_NUM_PLUS1}}. No credentials, tokens, or API keys in source code

### Git practices

{{GIT_NUM}}. Commit message is clear and descriptive
{{GIT_NUM_PLUS1}}. Co-author line present if AI-generated
{{GIT_NUM_PLUS2}}. Commit is atomic — one logical change, not a batch of unrelated edits

---

## Section B — Conditional checks

These checks apply only when the stated conditions are met.
If the condition does not apply, output `⏭️ N: [condition not met]`.

### TDD methodology
**Condition:** any new feature code or tests staged

{{TDD_NUM}}. Tests exist for the new/changed behaviour
{{TDD_NUM_PLUS1}}. Tests were written before or alongside implementation (not after)

### Dependencies
**Condition:** dependency manifest or lock file is staged

{{DEP_NUM}}. New dependencies have compatible licences (no GPL/AGPL unless explicitly approved)

---

## Project-specific checks

<!-- Add project-specific checks here. Examples:

### Documentation
Condition: always

N. Relevant docs updated if behaviour changed
N. No new documentation files created unless explicitly requested

### WordPress projects
Condition: PHP files staged

N. Nonces on all form submissions
N. current_user_can() before privileged operations
N. No direct SQL — use $wpdb->prepare()

### API changes
Condition: route handlers staged

N. New endpoints have tests
N. Error responses are consistent with existing API

-->

---

## Approval protocol

### If REJECT

- List every failing item with its number and required fix
- Do NOT write the approval flag
- Respond: `❌ BLOCKED — [N violations listed]`

### If APPROVE

All Section A items must pass. All applicable Section B items must pass.

1. Run: `Bash({ command: "date +%s" })` — note the timestamp
2. Write the approval flag:
   ```
   Write({
     file_path: "{{PROJECT_ROOT}}/reviewer-approved",
     content: "<timestamp>"
   })
   ```
3. Respond: `✅ APPROVED — approval flag written. Main agent may now stage and commit.`

**Only the reviewer agent writes `{{PROJECT_ROOT}}/reviewer-approved`. The main agent must not write it.**
The integrity of the two-factor system depends on this separation.

### Transparency

At every step, surface what you are doing in chat — which commands you ran, what
each item result was, and whether you wrote or withheld the approval flag.
The user monitors the conversation as the audit trail.
